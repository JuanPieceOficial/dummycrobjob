{
  "name": "dummy-render-pinger",
  "version": "1.0.0",
  "description": "A simple Node.js server to ping another Render service.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.18.2" // Si después de esta línea va otra dependencia y falta la coma, daría error
    "node-cron": "^3.0.0",
    "node-fetch": "^2.6.7"
  },
  "engines": {
    "node": "18.x" 
  }
}
